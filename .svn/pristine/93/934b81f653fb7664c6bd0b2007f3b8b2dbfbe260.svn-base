using System;
using System.Collections.Generic;
using System.Text;
using System.Data;

namespace IDAL
{
   public interface customerDal
    {
        int CustomerAdd(Model.tab_customers customer);
        DataTable CustomerSelect(Model.tab_customers cus);
        Model.tab_customers getCustomer(Model.tab_customers cus);
        DataTable CustomerSelectAll();
        int Delete(int customerID);
        int update(Model.tab_customers cus);
       DataTable CustomerSelect(string ss);
       double getbalance(int id);
    }
}
